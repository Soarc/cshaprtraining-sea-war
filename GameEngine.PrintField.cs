using System;

namespace Foundation.Hub256.Seawar
{
    partial class GameEngine
    {
        /// <summary>
        /// Prints the ship field. Possible values of fields are
        /// 0 - if position is not hitted yet
        /// 1 - if in position is ship
        /// 2 - if in position is hitted ship
        /// 3 - if in position is dead ship
        /// 4 - if in position is hitted sea
        /// 5 - if in position can't be a ship (i.e when ship)
        /// </summary>
        public void PrintField(int[,] field)
        {
            throw new NotImplementedException();
        }
    }
}